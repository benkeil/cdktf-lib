name: 'CodeQL Vulnerability Code Scan'

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '45 11 * * 1'

jobs:
  analyze:
    name: 'Analyze'
    uses: 'otto-ec/pdh-da_github-actions/.github/workflows/code-analysis.yaml@main'
    secrets: 'inherit'
    with:
      java_distribution: 'corretto'
      java_version: '17'
